# Required on Vercel for embedding queries (and build-time indexing).
# Create a free token at https://huggingface.co/settings/tokens
HF_API_TOKEN=

# Optional overrides
# EMBEDDINGS_MODEL=sentence-transformers/all-MiniLM-L6-v2
# PDF_DIR=/custom/path/to/pdfs
# VECTORSTORE_DIR=/custom/path/to/vectorstore
